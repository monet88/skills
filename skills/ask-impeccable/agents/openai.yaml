interface:
  display_name: "Ask Impeccable"
  short_description: "Coordinates Impeccable UI command batches in coding workers."
  default_prompt: "Use $ask-impeccable to coordinate explicit /impeccable commands in coding workers: start with /impeccable init, batch known serial commands when safe, and split only at real decision or failure boundaries."

policy:
  allow_implicit_invocation: true
  products:
    - chatgpt
    - codex
